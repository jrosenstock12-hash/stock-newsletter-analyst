# Copy to Streamlit Cloud → Settings → Secrets (not to your repo)

OPENAI_API_KEY = "sk-proj-your-key-here"
OPENAI_MODEL = "gpt-4o-mini"

# Optional: password-protect the app
# APP_PASSWORD = "choose-a-strong-password"
